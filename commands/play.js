const ytdl = require("ytdl-core");
const ytSearch = require("yt-search");

module.exports = {
    name: "play",
    description: "plays a video from youtube",
    execute(message){
        const textChannel = message.member.text.channel;
    }
}